# travel-time-estimator

A simple C program that calculates the duration of a trip based on distance and speed.

Features:
- Takes distance (km) and average speed (km/h) as input.
- Calculates total time in hours and minutes.
- Includes basic error handling for invalid (zero or negative) inputs.

Formula used:
Time = Distance / Speed
